 
 const init = {
    isLoading : false,
    isError:false,
    admindata:[]

 }
 export const reducer = (state = init,{type,payload})=>{
      switch(type){

        default:
            return state;
      }
 }